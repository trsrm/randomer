<nav class="navbar navbar-default navbar-static-top navbar-inverse">
    <div class="container">
        <ul class="nav navbar-nav">
            <li class="active">
                <a href="/"><span class="glyphicon glyphicon-random"></span>&nbsp;&nbsp;Randomer</a>
            </li>
        </ul>
        <% if (result) { %>
            <p class="navbar-text navbar-right"><%- result %></p>
        <% } else { %>
            <form class="navbar-form navbar-right" action="" method="post">
                <div class="form-group">
                    <label>Введи дані:</label>
                </div>
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Логін" name="login" required>
                </div>
                <div class="form-group">
                    <input type="password" class="form-control" placeholder="Пароль" name="password" required>
                </div>
                <button type="submit" class="btn btn-default">Отримати</button>
            </form>
        <% } %>
    </div>
</nav>
